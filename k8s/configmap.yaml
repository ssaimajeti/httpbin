apiVersion: v1
kind: ConfigMap
metadata:
  name: httpbin-config
data:
  AUDIT_DB_PORT: {{ .Values.httpbin.db.port | quote }}
  AUDIT_DB_NAME: {{ .Values.httpbin.db.name | quote }}
  AUDIT_RETENTION_DAYS: {{ .Values.httpbin.audit.retentionDays | quote }}